#!/bin/bash

if [ `nmcli networking connectivity` != "full" ]
then
	echo -e ? 📦
	exit 1
fi

function writestatus() {
	if [[ "$count" != "0" ]]; then
		echo -e $count 📦
	fi
}

{ checkupdates > /dev/null && count=`checkupdates | sed '/\[ignored\]/d' | wc -l` && writestatus $count; } &
