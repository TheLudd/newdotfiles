#!/bin/bash

if [ `nmcli networking connectivity` != "full" ]
then
	echo -e ? \\uf49e
	exit 1
fi

function writestatus() {
	if [[ "$count" != "0" ]]; then
		echo -e $count \\uf49e
	fi
}

{ sudo pacman -Sy > /dev/null && count=`pacman -Qu | wc -l` && writestatus $count; } &
