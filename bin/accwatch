#!/bin/bash
file=$@
if [[ -a "nodemon.json" ]]; then
	nodemon -q -e json,coffee,js,feature,feature-template,mjs,cjs -x "yarn -s accept $file"
else
	nodemon -q -i dist -i test -e json,coffee,js,feature,feature-template,mjs,cjs -x "yarn -s accept $file"
fi
