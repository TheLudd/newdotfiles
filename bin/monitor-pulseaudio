#!/bin/bash
$HOME/bin/calculate-sound

pactl subscribe | grep --line-buffered -E "change.*(sink|source|server)" | while read -r line; do
   $HOME/bin/calculate-sound
   kill -40 $(pidof dwmblocks)
done
