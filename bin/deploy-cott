#!/bin/bash
set -e
cd ~/code/northlight/
yarn
yarn transpile
cd framework
yarn vite build
sudo systemctl start docker.service
docker build . -t mediatool/cott
docker push mediatool/cott
ssh staging-bff /var/mediatool-scripts/deploy-cott
