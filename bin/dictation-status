#!/bin/sh
# Output mic emoji with backend name when dictation is active

if [ -f /tmp/whisper-dictation.pid ]; then
    echo "🎤 (whisper)"
elif pgrep -f "[n]erd-dictation begin" > /dev/null; then
    echo "🎤 (vosk)"
fi
